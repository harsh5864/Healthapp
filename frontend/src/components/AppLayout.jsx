import { NavLink, Outlet, Link, useNavigate } from 'react-router-dom';
import { useAuth } from '../context/AuthContext';

const nav = [['/app','Overview','⌂'],['/app/food','Food scanner','🍎'],['/app/chat','Health chat','✦'],['/app/wellness','Wellness','◒'],['/app/history','History','◷'],['/app/settings','Settings','⚙']];
export default function AppLayout() { const { user, logout } = useAuth(); const navigate = useNavigate(); return <div className="app-shell"><aside className="app-sidebar"><Link className="brand" to="/"><span className="brand__mark">✦</span><span>AI Health <strong>Companion</strong></span></Link><nav>{nav.map(([to,label,icon])=><NavLink key={to} to={to} end={to==='/app'} className={({isActive})=>isActive?'active':''}><span>{icon}</span>{label}</NavLink>)}</nav><div className="sidebar-bottom"><NavLink to="/app/profile">◉ <span>Profile</span></NavLink><button onClick={()=>{logout();navigate('/login');}}>↪ <span>Sign out</span></button></div></aside><main className="app-main"><header className="app-topbar"><div><span className="topbar-kicker">AI HEALTH COMPANION</span><span className="mobile-brand">Your private wellness space</span></div><div className="topbar-user"><span className="avatar">{user?.name?.[0]?.toUpperCase() || 'U'}</span><span className="topbar-name">{user?.name || 'Member'}</span></div></header><Outlet /></main></div>; }
