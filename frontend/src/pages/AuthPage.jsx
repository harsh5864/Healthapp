import { useState } from 'react'; import { Link, useLocation, useNavigate } from 'react-router-dom'; import { useAuth } from '../context/AuthContext';
export default function AuthPage({ mode='login' }) { const isLogin=mode==='login'; const [form,setForm]=useState({name:'',email:'',password:'',confirmPassword:''}); const [error,setError]=useState(''); const { login,register,loading }=useAuth(); const navigate=useNavigate(); const location=useLocation(); const submit=async(e)=>{e.preventDefault();setError('');try{await (isLogin?login({email:form.email,password:form.password}):register(form));navigate(location.state?.from||'/app');}catch(err){setError(err.response?.data?.message||'Unable to complete that request. Please check your details.');}}; return <main className="auth-page"><div className="auth-art"><Link className="brand" to="/"><span className="brand__mark">✦</span><span>AI Health <strong>Companion</strong></span></Link><div><p className="eyebrow">A calmer next step</p><h1>Care for your everyday wellbeing, in one place.</h1><p>General information, visible food checks and personal trends—with clear limits and your privacy in mind.</p></div><small>Not a diagnosis or replacement for professional care.</small></div><section className="auth-panel"><Link to="/" className="back-link">← Back home</Link><p className="eyebrow">{isLogin?'Welcome back':'Create your account'}</p><h2>{isLogin?'Sign in to your companion':'Start your private wellness space'}</h2><p className="auth-subtitle">{isLogin?'Continue your health and wellness journey.':'A few details are all you need to begin.'}</p><form onSubmit={submit}>{!isLogin&&<label>Name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Your name" /></label>}<label>Email<input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="you@example.com" /></label><label>Password<input required type="password" minLength="8" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="At least 8 characters" /></label>{!isLogin&&<label>Confirm password<input required type="password" value={form.confirmPassword} onChange={e=>setForm({...form,confirmPassword:e.target.value})} placeholder="Repeat your password" /></label>}{error&&<div className="form-error">{error}</div>}<button className="button button--primary full" disabled={loading}>{loading?'Please wait…':isLogin?'Sign in':'Create account'}</button></form><p className="auth-switch">{isLogin?"Don't have an account? ":'Already have an account? '}<Link to={isLogin?'/register':'/login'}>{isLogin?'Create one':'Sign in'}</Link></p><small className="auth-disclaimer">By continuing, you acknowledge this is general informational support, not medical advice.</small></section></main>; }
