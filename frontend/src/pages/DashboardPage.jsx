import { useEffect, useState } from 'react'; import { Link } from 'react-router-dom'; import { dashboardApi, foodApi, wellnessApi } from '../services/appServices'; import { useAuth } from '../context/AuthContext'; import FeatureCard from '../components/FeatureCard';
export default function DashboardPage(){const {user}=useAuth();const [data,setData]=useState(null);const [food,setFood]=useState([]);const [wellness,setWellness]=useState(null);useEffect(()=>{Promise.all([dashboardApi.summary(),foodApi.history(),wellnessApi.summary()]).then(([d,f,w])=>{setData(d.data);setFood(f.data);setWellness(w.data);}).catch(()=>{});},[]);return <section className="page-content"><div className="page-heading"><div><p className="eyebrow">Your overview</p><h1>Good to see you, {user?.name?.split(' ')[0] || 'there'}.</h1><p>Use today&apos;s small check-ins to build a clearer picture over time.</p></div><Link className="button button--primary" to="/app/wellness">Daily check-in <span>→</span></Link></div><div className="metric-grid"><Metric label="Food scans" value={data?.foodScans??'—'} icon="🍎"/><Metric label="Conversations" value={data?.conversations??'—'} icon="✦"/><Metric label="Current mood" value={data?.currentMood?`${data.currentMood}/10`:'—'} icon="◒"/><Metric label="Average sleep" value={data?.averageSleep?`${Number(data.averageSleep).toFixed(1)}h`:'—'} icon="☾"/></div><div className="dashboard-columns"><div><div className="section-title"><h2>Your tools</h2><span>Private by design</span></div><div className="dashboard-feature-grid"><FeatureCard icon="🍎" title="Food freshness" body="Check visible characteristics and keep a history of your scans." to="/app/food" action="Scan food"/><FeatureCard icon="💬" title="Health chat" body="Ask for general health information with red-flag awareness." to="/app/chat" action="Ask health AI"/><FeatureCard icon="🧠" title="Mental wellness" body="Record mood, stress, energy and sleep to see your own patterns." to="/app/wellness" action="Check in"/></div></div><div className="side-panel"><div className="section-title"><h2>Recent scan</h2><Link to="/app/history">View all</Link></div>{food[0]?<div className="recent-item"><span className="recent-emoji">🍎</span><div><b>{food[0].foodName}</b><small>{food[0].condition}</small></div><strong>{food[0].freshnessScore}%</strong></div>:<div className="empty-state">Your analyzed food will appear here.</div>}<div className="observation"><span>✦</span><div><b>Wellness observation</b><p>{wellness?.analysis?.analysis||'Complete a check-in to receive a personal, non-diagnostic trend observation.'}</p></div></div></div></div><p className="medical-note">AI-generated information is not medical diagnosis or treatment. Seek professional care for concerning symptoms.</p></section>}
function Metric({label,value,icon}){return <div className="metric-card"><span>{icon}</span><small>{label}</small><strong>{value}</strong></div>}
