# Netlify configuration for AI Health Companion frontend

[build]
  base = "frontend"
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

# Support React Router Single Page App routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
